@model BearToyWebsiteBack.Models.Product

@{
    ViewData["Title"] = "新增商品";
}

<div class="fade-in">
    <!-- 頁面標題和導覽 -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">
                <i class="bi bi-plus-circle text-primary me-2"></i>
                新增商品
            </h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item"><a asp-action="Index">商品管理</a></li>
                    <li class="breadcrumb-item active" aria-current="page">新增商品</li>
                </ol>
            </nav>
        </div>
        <div>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left me-2"></i>
                返回列表
            </a>
        </div>
    </div>

    <!-- 表單內容 -->
    <form asp-action="Create" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
        
        <div class="row">
            <!-- 基本資訊 -->
            <div class="col-xl-8">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-info-circle me-2"></i>
                            基本資訊
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="Sku" class="form-label">
                                    商品編號 <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Sku" class="form-control" placeholder="請輸入商品編號" />
                                <span asp-validation-for="Sku" class="text-danger"></span>
                            </div>
                            
                            <div class="col-md-6">
                                <label asp-for="Name" class="form-label">
                                    商品名稱 <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Name" class="form-control" placeholder="請輸入商品名稱" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                            
                            <div class="col-12">
                                <label asp-for="Description" class="form-label">
                                    商品描述 <span class="text-danger">*</span>
                                </label>
                                <textarea asp-for="Description" class="form-control" rows="5" placeholder="請詳細描述商品特色、規格等資訊"></textarea>
                                <span asp-validation-for="Description" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 定價與庫存 -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-currency-dollar me-2"></i>
                            定價與庫存
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="Price" class="form-label">
                                    售價 (NT$) <span class="text-danger">*</span>
                                </label>
                                <div class="input-group">
                                    <span class="input-group-text">NT$</span>
                                    <input asp-for="Price" class="form-control" type="number" step="0.01" min="0" placeholder="0.00" />
                                </div>
                                <span asp-validation-for="Price" class="text-danger"></span>
                            </div>
                            
                            <div class="col-md-6">
                                <label asp-for="StockQuantity" class="form-label">
                                    庫存數量 <span class="text-danger">*</span>
                                </label>
                                <div class="input-group">
                                    <input asp-for="StockQuantity" class="form-control" type="number" min="0" placeholder="0" />
                                    <span class="input-group-text">件</span>
                                </div>
                                <span asp-validation-for="StockQuantity" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 商品主圖 -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-image me-2"></i>
                            商品主圖
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label class="form-label">上傳主圖</label>
                            <input type="file" name="mainImage" class="form-control" accept=".jpg,.jpeg,.png,.gif,.webp,image/jpeg,image/jpg,image/png,image/gif,image/webp" id="mainImageInput" />
                            <div class="form-text">
                                <i class="bi bi-info-circle me-1"></i>
                                選擇一張圖片作為商品主圖。建議尺寸：正方形或 4:3 比例。
                            </div>
                        </div>

                        <div id="mainImagePreview" style="display: none;">
                            <h6 class="mb-2">主圖預覽</h6>
                            <div id="mainImagePreviewContainer"></div>
                        </div>
                    </div>
                </div>

                <!-- 商品內容圖 -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-images me-2"></i>
                            商品內容圖
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label class="form-label">上傳內容圖</label>
                            <input type="file" name="galleryImages" class="form-control" multiple accept=".jpg,.jpeg,.png,.gif,.webp,image/jpeg,image/jpg,image/png,image/gif,image/webp" id="galleryImageInput" />
                            <div class="form-text">
                                <i class="bi bi-info-circle me-1"></i>
                                一個商品最多可以有 9 張內容圖。圖片將自動調整為 500x500px，並產生 200x200px 的縮圖。
                            </div>
                        </div>

                        <div id="galleryImagePreview" style="display: none;">
                            <h6 class="mb-2">內容圖預覽</h6>
                            <div id="galleryImagePreviewContainer" class="row g-3"></div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 側邊欄 -->
            <div class="col-xl-4">
                <!-- 分類設定 -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-tags me-2"></i>
                            分類設定
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="CategoryId" class="form-label">
                                主分類 <span class="text-danger">*</span>
                            </label>
                            <select asp-for="CategoryId" class="form-select" asp-items="ViewBag.CategoryId" id="categorySelect">
                                <option value="">請選擇主分類</option>
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="SubCategoryId" class="form-label">子分類</label>
                            <select asp-for="SubCategoryId" class="form-select" id="subCategorySelect">
                                <option value="">請先選擇主分類</option>
                            </select>
                            <span asp-validation-for="SubCategoryId" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <!-- 商品設定 -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-gear me-2"></i>
                            商品設定
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" asp-for="IsFeatured" id="featuredSwitch" />
                            <label class="form-check-label" for="featuredSwitch">
                                <strong>精選商品</strong>
                                <small class="d-block text-muted">精選商品會在首頁顯示</small>
                            </label>
                        </div>
                    </div>
                </div>

                <!-- 操作按鈕 -->
                <div class="card">
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-check-circle me-2"></i>
                                新增商品
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-x-circle me-2"></i>
                                取消
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script>
        $(document).ready(function() {
            // 分類連動功能
            $('#categorySelect').change(function() {
                var categoryId = $(this).val();
                $('#subCategorySelect').empty();
                $('#subCategorySelect').append('<option value="">請選擇子分類</option>');
                
                if (categoryId) {
                    $.ajax({
                        url: '@Url.Action("GetSubCategories", "Products")',
                        type: 'GET',
                        data: { categoryId: categoryId },
                        success: function(data) {
                            $.each(data, function(index, item) {
                                $('#subCategorySelect').append('<option value="' + item.value + '">' + item.text + '</option>');
                            });
                        }
                    });
                } else {
                    $('#subCategorySelect').append('<option value="">請先選擇主分類</option>');
                }
            });
            
            // 主圖預覽功能
            $('#mainImageInput').change(function() {
                var files = this.files;
                var previewContainer = $('#mainImagePreview');
                var previewContent = $('#mainImagePreviewContainer');

                previewContent.empty();

                if (files.length > 0 && files[0].type.match('image.*')) {
                    previewContainer.show();

                    var reader = new FileReader();
                    reader.onload = function(e) {
                        var previewHtml = `
                            <div class="col-md-4">
                                <div class="card">
                                    <img src="${e.target.result}" class="card-img-top" style="height: 200px; object-fit: cover;">
                                    <div class="card-body p-2 text-center">
                                        <span class="badge bg-primary">主圖</span>
                                        <small class="d-block text-muted mt-1">${files[0].name}</small>
                                    </div>
                                </div>
                            </div>
                        `;
                        previewContent.html(previewHtml);
                    };
                    reader.readAsDataURL(files[0]);
                } else {
                    previewContainer.hide();
                }
            });

            // 內容圖預覽功能
            $('#galleryImageInput').change(function() {
                var files = this.files;
                var previewContainer = $('#galleryImagePreview');
                var previewContent = $('#galleryImagePreviewContainer');

                previewContent.empty();

                if (files.length > 0) {
                    previewContainer.show();

                    var maxFiles = Math.min(files.length, 9);
                    for (var i = 0; i < maxFiles; i++) {
                        if (files[i].type.match('image.*')) {
                            var reader = new FileReader();
                            reader.onload = (function(file, index) {
                                return function(e) {
                                    var fileSizeKB = Math.round(file.size / 1024);
                                    var previewHtml = `
                                        <div class="col-lg-3 col-md-4 col-sm-6">
                                            <div class="card">
                                                <img src="${e.target.result}" class="card-img-top" style="height: 150px; object-fit: cover;">
                                                <div class="card-body p-2">
                                                    <span class="badge bg-success">內容圖</span>
                                                    <small class="text-muted d-block">${file.name}</small>
                                                    <small class="text-info">${fileSizeKB}KB</small>
                                                </div>
                                            </div>
                                        </div>
                                    `;
                                    previewContent.append(previewHtml);
                                };
                            })(files[i], i);
                            reader.readAsDataURL(files[i]);
                        }
                    }

                    if (files.length > 9) {
                        // 顯示警告訊息
                        var alertHtml = `
                            <div class="alert alert-warning alert-dismissible fade show mt-3" role="alert">
                                <i class="bi bi-exclamation-triangle me-2"></i>
                                只能上傳最多9張內容圖，已自動選取前9張圖片。
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        `;
                        previewContainer.append(alertHtml);
                    }
                } else {
                    previewContainer.hide();
                }
            });
            
            // 表單驗證增強
            $('form').submit(function(e) {
                var isValid = true;
                var firstErrorField = null;
                
                // 檢查必填欄位
                $('input[required], select[required], textarea[required]').each(function() {
                    if (!$(this).val().trim()) {
                        isValid = false;
                        $(this).addClass('is-invalid');
                        if (!firstErrorField) {
                            firstErrorField = $(this);
                        }
                    } else {
                        $(this).removeClass('is-invalid');
                    }
                });
                
                if (!isValid) {
                    e.preventDefault();
                    if (firstErrorField) {
                        firstErrorField.focus();
                    }
                    
                    // 顯示錯誤提示
                    if ($('.alert-danger').length === 0) {
                        $('form').prepend(`
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <i class="bi bi-exclamation-triangle me-2"></i>
                                請填寫所有必填欄位
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        `);
                    }
                }
            });
            
            // 移除無效狀態當用戶開始輸入
            $('input, select, textarea').on('input change', function() {
                $(this).removeClass('is-invalid');
            });
        });
    </script>
}